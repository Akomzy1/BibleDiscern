import { OnboardingFlow } from '@/components/onboarding/OnboardingFlow';

// Own layout (no tab bar): the flow renders its full-bleed nave800 shell.
export default function OnboardingPage() {
  return <OnboardingFlow />;
}
